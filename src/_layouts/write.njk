---
layout: base.njk
grid: writer
eleventyComputed:
  book: '{{ doc.data.title or doc.slug }}'
  title: 'Write {{ doc.data.title or doc.slug }}'
---

<label for="typr">
  Please write
  <strong>{{ book }}</strong>
  and nothing else:
</label>
<textarea name="{{ doc.slug }}" id="typr"></textarea>
<p>
  Remember: writing {{ book }} is not magic,
  you just need to type all the right characters
  in the right order.
</p>

{% if doc %}
  <script>
    {% include "write.js" %}
    document.onload = write(`{{ doc.content | safe }}`);
  </script>
{% endif %}
