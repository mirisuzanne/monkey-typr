<!DOCTYPE html>
<html lang="en" data-mode="auto">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or site.title }}</title>
  <meta name="description" content="{{ desc or site.description }}">

  <link rel="stylesheet" href="/css/main.css">
  <link rel="shortcut icon" href="data:image/svg+xml,%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20viewBox%3D%220%200%20100%20100%22%3E%3Ctext%20y%3D%22.9em%22%20font-size%3D%2290%22%3E%F0%9F%99%88%3C%2Ftext%3E%3C%2Fsvg%3E" type="image/svg+xml">

  <meta property="og:locale" content="{{ site.og.locale }}" />
  <meta property="og:type" content="{{ site.og.type }}" />
  <meta property="og:title" content="{{ site.title }}{% if title %} » {{ title }}{% endif %}" />
  <meta property="og:image" content="{{ site.og.img | absoluteUrl(site.url) }}" />
  <meta property="og:image:alt" content="{{ site.og.alt }}" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="360" />
  <meta property="og:description" content="{{ desc or site.description }}" />
  <meta property="og:url" content="{{ page.url | absoluteUrl(site.url) }}" />
  <meta property="og:site_name" content="{{ site.title }}" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:description" content="{{ desc or site.description }}" />
  <meta name="twitter:title" content="{{ site.title }}{% if title %} » {{ title }}{% endif %}" />
  <meta name="twitter:domain" content="{{ site.url }}" />
  <meta name="twitter:creator" content="@TerribleMia" />
</head>
<body data-grid="{{ grid or 'default'}}">
  <header>
    {% if book %}
      <a href="/" rel="home">
        <span class="logo">{{ site.logo }}</span>
        {{ site.title }}
      </a>
      <h1 id="book">
        {{ book }}
        {% if doc.data.author %}
          by {{ doc.data.author }}
        {% endif %}
      </h1>
    {% else %}
      <h1>
        <span class="logo">{{ site.logo }}</span>
        {{ site.title }}
      </h1>
    {% endif %}
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <div class="copy">
      <strong><a href="https://monkey.writr.art">monkey.writr.art</a></strong>
      <div>
        Copyright &copy; {% year %}
        <a href="{{ site.author.url }}">{{ site.author.name }}</a>
      </div>
    </div>
    <div class="meta">
      <button data-toggle="mode" data-states="auto light dark" data-state="auto">colors</button>
    </div>
  </footer>

  <script>
    {% include "toggle.js" %}
  </script>

  {% if doc %}
    <script>
      {% include "write.js" %}
      write(`{{ doc.content | safe }}`);
    </script>
  {% endif %}
</body>
</html>
