<!DOCTYPE html>
<html lang="en" data-mode="auto">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or site.title }}</title>
  <link rel="stylesheet" href="/css/main.css">
  <link rel="shortcut icon" href="data:image/svg+xml,%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20viewBox%3D%220%200%20100%20100%22%3E%3Ctext%20y%3D%22.9em%22%20font-size%3D%2290%22%3E%F0%9F%99%88%3C%2Ftext%3E%3C%2Fsvg%3E" type="image/svg+xml">
</head>
<body data-grid="{{ grid or 'default'}}">
  <header>
    {% if book %}
      <a href="/" rel="home">
        <span class="logo">{{ site.logo }}</span>
        {{ site.title }}
      </a>
      <h1 id="book">
        {{ book }}
        {% if doc.data.author %}
          by {{ doc.data.author }}
        {% endif %}
      </h1>
    {% else %}
      <h1>
        <span class="logo">{{ site.logo }}</span>
        {{ site.title }}
      </h1>
    {% endif %}
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <div class="copy">
      <strong><a href="https://monkey.writr.art">monkey.writr.art</a></strong>
      <div>
        Copyright &copy; {% year %}
        <a href="{{ site.author.url }}">{{ site.author.name }}</a>
      </div>
    </div>
    <div class="meta">
      <button data-toggle="mode" data-states="auto light dark" data-state="auto">colors</button>
    </div>
  </footer>

  <script>
    {% include "toggle.js" %}
  </script>

  {% if doc %}
    <script>
      {% include "write.js" %}
      document.onload = write(`{{ doc.content | safe }}`);
    </script>
  {% endif %}
</body>
</html>
